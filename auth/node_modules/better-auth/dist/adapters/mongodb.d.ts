import { Db } from 'mongodb';
import { W as Where } from '../index-PjYlcsPD.js';
import 'kysely';
import '../index-JM-i6hLs.js';
import 'arctic';
import 'zod';
import '../helper-DPDj8Nix.js';
import 'better-call';
import 'better-sqlite3';
import 'mysql2';

declare const mongodbAdapter: (mongo: Db) => {
    id: string;
    create<T, R = T>(data: {
        model: string;
        data: T;
        select?: string[];
    }): Promise<any>;
    findOne<T>(data: {
        model: string;
        where: Where[];
        select?: string[];
    }): Promise<any>;
    findMany<T>(data: {
        model: string;
        where?: Where[];
    }): Promise<any[]>;
    update<T>(data: {
        model: string;
        where: Where[];
        update: Record<string, any>;
    }): Promise<any>;
    delete<T>(data: {
        model: string;
        where: Where[];
    }): Promise<void>;
};

export { mongodbAdapter };
